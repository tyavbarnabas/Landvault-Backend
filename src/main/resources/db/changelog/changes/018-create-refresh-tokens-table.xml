<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <!--
        identity.internal.domain.RefreshToken — session storage. Stores
        only token_hash, never the raw token: a refresh token in the
        database is a live credential, so a database leak must not be an
        account takeover. replaced_by chains rotations.

        TODO: the frontend currently holds the access token in localStorage
        (a flagged XSS exposure). Moving refresh tokens to httpOnly cookies
        is the intended direction, pending backend cookie/CORS decisions
        not settled yet.

        Column shapes were taken directly from Hibernate's own DDL export
        for the current entity mapping (jakarta.persistence.schema-generation
        with ddl-auto=none), so they match exactly what ddl-auto=validate
        expects. Don't hand-tune a column without re-exporting and
        re-checking against that mapping.
    -->
    <changeSet id="018-create-refresh-tokens-table" author="landvault">
        <sql>
            CREATE TABLE refresh_tokens (
                id             uuid NOT NULL,
                tenant_id      uuid,
                branch_id      uuid,
                created_at     timestamp(6) with time zone NOT NULL,
                updated_at     timestamp(6) with time zone,
                created_by     varchar(255),
                updated_by     varchar(255),
                deleted        boolean NOT NULL,

                user_id        uuid NOT NULL,
                token_hash     varchar(255) NOT NULL,
                expires_at     timestamp(6) with time zone NOT NULL,
                revoked_at     timestamp(6) with time zone,
                replaced_by    uuid,
                device         varchar(255),
                user_agent     varchar(255),
                ip_address     varchar(255),

                PRIMARY KEY (id)
            );
        </sql>
        <rollback>
            DROP TABLE refresh_tokens;
        </rollback>
    </changeSet>

    <changeSet id="018-create-refresh-tokens-fk-indexes" author="landvault">
        <addForeignKeyConstraint
                baseTableName="refresh_tokens" baseColumnNames="user_id"
                constraintName="fk_refresh_tokens_user_id"
                referencedTableName="users" referencedColumnNames="id"/>
        <addForeignKeyConstraint
                baseTableName="refresh_tokens" baseColumnNames="replaced_by"
                constraintName="fk_refresh_tokens_replaced_by"
                referencedTableName="refresh_tokens" referencedColumnNames="id"/>
        <createIndex indexName="idx_refresh_tokens_user_id" tableName="refresh_tokens">
            <column name="user_id"/>
        </createIndex>
        <createIndex indexName="idx_refresh_tokens_replaced_by" tableName="refresh_tokens">
            <column name="replaced_by"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
